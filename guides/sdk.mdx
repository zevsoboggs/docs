---
title: 'SDK и библиотеки'
description: 'Официальные SDK для интеграции с Love&Pay'
icon: 'code'
---

## Официальные SDK

Мы активно работаем над созданием официальных SDK для упрощения интеграции с Love&Pay API.

<Info>
  **Скоро:** Официальный SDK на TypeScript уже в разработке и будет доступен в ближайшее время!
</Info>

## TypeScript SDK (Coming Soon)

<Card title="@loveandpay/sdk" icon="js">
  Первый официальный SDK будет выпущен для **TypeScript/JavaScript** — самого популярного языка для веб-разработки.
</Card>

### Что будет в SDK

<CardGroup cols={2}>
  <Card title="Типизация" icon="shield-check">
    Полная типизация TypeScript для безопасной разработки
  </Card>
  <Card title="Автодополнение" icon="wand-magic-sparkles">
    IDE подсказки для всех методов и параметров
  </Card>
  <Card title="Обработка ошибок" icon="triangle-exclamation">
    Встроенная обработка ошибок и retry-логика
  </Card>
  <Card title="Валидация" icon="check-double">
    Проверка параметров перед отправкой запросов
  </Card>
</CardGroup>

### Пример использования (Preview)

```typescript
import { LoveAndPay } from '@loveandpay/sdk';

// Инициализация клиента
const client = new LoveAndPay({
  apiKey: process.env.LOVEANDPAY_API_KEY,
});

// Создание счёта
const invoice = await client.invoices.create({
  amount: 1500,
  currency: 'RUB',
  description: 'Оплата заказа #12345',
  customer: {
    email: 'client@example.com',
    name: 'Иван Иванов',
  },
});

console.log(`Ссылка для оплаты: ${invoice.paymentUrl}`);

// Получение списка счетов
const invoices = await client.invoices.list({
  status: 'PAID',
  limit: 10,
});

// Настройка вебхука
const webhook = await client.webhooks.create({
  url: 'https://yoursite.com/webhook',
  events: ['INVOICE_PAID', 'INVOICE_EXPIRED'],
});

// Верификация подписи вебхука
const isValid = client.webhooks.verifySignature(
  rawBody,
  signature,
  webhookSecret
);
```

### Поддерживаемые платформы

| Платформа | Поддержка |
|-----------|-----------|
| Node.js 18+ | ✅ |
| Bun | ✅ |
| Deno | ✅ |
| Edge Functions (Vercel, Cloudflare) | ✅ |
| Браузер | ⚠️ Только публичные методы |

## Планы по SDK

<Steps>
  <Step title="TypeScript SDK">
    **Статус:** В разработке 🚧

    Первый официальный SDK с полной типизацией
  </Step>
  <Step title="Python SDK">
    **Статус:** Планируется 📋

    Для интеграции с Django, FastAPI, Flask
  </Step>
  <Step title="PHP SDK">
    **Статус:** Планируется 📋

    Для Laravel, Symfony и других фреймворков
  </Step>
  <Step title="Go SDK">
    **Статус:** Рассматривается 💭

    Для высоконагруженных систем
  </Step>
</Steps>

## Пока SDK не вышел

До выхода официального SDK вы можете использовать любой HTTP-клиент:

<CodeGroup>

```typescript TypeScript (fetch)
const response = await fetch('https://loveandpay.io/api/v1/invoices', {
  method: 'POST',
  headers: {
    'x-api-key': process.env.LOVEANDPAY_API_KEY!,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    amount: 1500,
    currency: 'RUB',
  }),
});

const invoice = await response.json();
```

```typescript TypeScript (axios)
import axios from 'axios';

const client = axios.create({
  baseURL: 'https://loveandpay.io/api/v1',
  headers: {
    'x-api-key': process.env.LOVEANDPAY_API_KEY,
  },
});

const { data: invoice } = await client.post('/invoices', {
  amount: 1500,
  currency: 'RUB',
});
```

```python Python (requests)
import requests
import os

response = requests.post(
    'https://loveandpay.io/api/v1/invoices',
    headers={
        'x-api-key': os.environ['LOVEANDPAY_API_KEY'],
        'Content-Type': 'application/json',
    },
    json={
        'amount': 1500,
        'currency': 'RUB',
    }
)

invoice = response.json()
```

```php PHP (cURL)
<?php

$ch = curl_init('https://loveandpay.io/api/v1/invoices');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'x-api-key: ' . $_ENV['LOVEANDPAY_API_KEY'],
        'Content-Type: application/json',
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'amount' => 1500,
        'currency' => 'RUB',
    ]),
]);

$response = curl_exec($ch);
$invoice = json_decode($response, true);

curl_close($ch);
```

</CodeGroup>

## Хотите узнать первыми?

<Card title="Подпишитесь на обновления" icon="envelope" href="https://loveandpay.io">
  Оставьте email в личном кабинете и мы сообщим о выходе SDK. Или напишите нам в онлайн-чат на **loveandpay.io**.
</Card>

<Info>
  Есть идеи или пожелания по SDK? Напишите нам — мы учтём ваши предложения при разработке!
</Info>
